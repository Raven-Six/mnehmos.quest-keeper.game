# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri
src-tauri/target/
src-tauri/gen/

# MCP Server binaries (transpiled from rpg-mcp, deployed at runtime)
# Keep only the native sqlite module and entry point
src-tauri/binaries/server/
src-tauri/binaries/storage/
src-tauri/binaries/dsl/
src-tauri/binaries/worldgen/
src-tauri/binaries/workflow/
src-tauri/binaries/*.js
src-tauri/binaries/*.d.ts
src-tauri/binaries/*.js.map
src-tauri/binaries/*.d.ts.map

# Rust experiment (not part of main codebase)
rust-mcp-server/

Roo-Code
Lightweight-CRM---Invoice/

# Runtime database files (prevents Tauri file watcher rebuild loops)
*.db-shm
*.db-wal

# Database files (users start fresh, data stored in AppData)
*.db
# Deprecated components (old MCP servers)
_deprecated/

# Backup files
*.backup
*_BACKUP.*
*_BACKUP_*

# Windows reserved filenames (can't be deleted normally)
nul
con
prn
aux

# Developer-only agent orchestration docs (not for version control)
Agents/

# Temporary test directories
temp_test_mcp/
